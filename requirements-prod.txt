# Production dependencies for Render deploy.
# TensorFlow excluded — Render free tier (512MB) cannot fit it; RF-only in prod.
# Full requirements.txt is used for local training/development.
flask==3.0.3
gunicorn==23.0.0
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.4.2
joblib==1.4.2
vaderSentiment==3.3.2
yfinance>=1.3.0
curl_cffi>=0.7
