# Production dependencies for Render deploy.
# Full TensorFlow excluded (~500 MB, won't fit 512 MB free tier). Instead the
# Keras LSTM's weights are exported to ``models/lstm_weights.npz`` via
# ``src/export_lstm_weights.py`` and served with a pure-numpy forward pass
# (``src/lstm_numpy.py``). No extra binary dep required — numpy is already
# pulled in by pandas/scikit-learn.
# Full requirements.txt is used for local training/development.
flask==3.0.3
gunicorn==23.0.0
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.4.2
joblib==1.4.2
vaderSentiment==3.3.2
yfinance>=1.3.0
curl_cffi>=0.7
